# Tested direct dependency set for the v1.0 Stable release line.
# Keep requirements.txt as the compatibility contract and update these pins only
# after the Python 3.10/3.13 regression matrix succeeds.
streamlit==1.58.0
openai==2.44.0
python-dotenv==1.2.2
coverage==7.15.1
